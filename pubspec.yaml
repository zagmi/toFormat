name: to_format
description: Adds a toFormat instance method to big or decimal.
version: 1.0.0
repository: https://github.com/zagmi/to_format
homepage: https://github.com/zagmi/to_format

environment:
  sdk:  ">=3.0.1"

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
